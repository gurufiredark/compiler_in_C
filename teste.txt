#include <stdio.h>
#include <stdlib.h>
#include <Math.h>
int main() {
    int 9x;
    x = 10;
    if (x > 5) {
        x = x + 1;
    } else {
        x = x - 1;
    }
    return x;
}

int soma(int a, int b) {
    return a + b;
}